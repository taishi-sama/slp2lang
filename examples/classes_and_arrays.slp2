uses std_char;

type 
    test = class
        a: int32; 
    end;
    arr = array [0..2] of test;


begin
    var a: arr;
    a[0] := new test(1);
    a[1] := new test(2);
    a[2] := new test(3);

end.